import { WagonLoadRatioPipe } from './wagon-load-ratio.pipe';

describe('WagonLoadRatioPipe', () => {
  it('create an instance', () => {
    const pipe = new WagonLoadRatioPipe();
    expect(pipe).toBeTruthy();
  });
});
