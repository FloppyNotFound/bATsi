import { IsTimetableScheduledEqualReportedPipe } from './is-timetable-scheduled-equal-reported.pipe';

describe('IsTimetableScheduledEqualReportedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsTimetableScheduledEqualReportedPipe();
    expect(pipe).toBeTruthy();
  });
});
